# 业务接入接口说明

> 状态：与第一版实现一致  
> 适用范围：业务接入模块的 HTTP 接口、API Key 使用方式和联调步骤

本文是 [业务接入模块设计](./业务接入模块设计.md) 的接口附录，只描述调用方式，不重复
设计理由。所有接口统一挂载在 `/api` 前缀下，响应统一使用 `Result` 包装。

## 1. 环境配置

```text
TENANCY_ENABLED=true             启用租户能力；关闭后按全局模式运行
APPROVAL_ADMIN_KEY=管理接口临时密钥
```

`TENANCY_ENABLED=false` 时：

- 发起审批不要求 `X-API-Key`。
- 不查询任何 `tenant` 表，`init.sql` 中的租户表可以不存在。
- 不写入 `process_usage_record`。
- 业务动作仍然需要存在且处于 `ENABLED`，`execution_payload` 仍然正常校验。

## 2. 联调步骤

```text
1. 创建租户                              POST /api/admin/tenants
2. 为租户签发 API Key                    POST /api/admin/tenants/{tenant_id}/api-keys
3. 创建并发布审批流                      POST /api/admin/processes
                                        PUT  /api/admin/process-versions/{version_id}/graph
                                        POST /api/admin/process-versions/{version_id}/publish
4. 创建业务动作                          POST /api/admin/business-actions
5. 授权租户使用该审批流                  POST /api/admin/tenants/{tenant_id}/process-bindings
6. 授权租户使用该业务动作                POST /api/admin/tenants/{tenant_id}/business-action-bindings
7. 业务系统发起审批                      POST /api/processes/{process_id}/instances
8. 业务系统查询审批详情或时间线            GET  /api/approval-instances/{instance_id}
                                        GET  /api/approval-instances/{instance_id}/timeline
9. 管理端按租户查询使用记录                GET  /api/admin/tenants/{tenant_id}/process-usage-records
```

第 1 至第 6 步和第 9 步使用 `X-Admin-Key`，第 7、8 步使用 `X-API-Key`。

## 3. 发起审批

```text
POST /api/processes/{process_id}/instances
X-API-Key: appr_live_xxx
```

```json
{
  "business_key": "PAY-20260921-001",
  "title": "供应商付款申请",
  "applicant_person_id": "人员UUID",
  "action_code": "PAYMENT_EXECUTE",
  "approval_form": { "amount": 10000, "supplier_name": "示例供应商" },
  "execution_payload": { "payment_id": "PAY-20260921-001", "amount": 10000 }
}
```

- `business_key` 是业务系统自己的单据标识，租户内同一流程下重复使用同一个
  `business_key` 会命中幂等。
- `action_code` 可以为空，表示只完成审批不触发业务执行。
- `execution_payload` 按业务动作的请求参数 Schema 校验，`approval_form` 按流程版本的
  表单 Schema 校验，两者互不影响。

成功响应：

```json
{
  "instance_id": "审批实例UUID",
  "status": "RUNNING",
  "process_id": "审批流UUID",
  "process_version_id": "实际运行版本UUID",
  "process_version_no": 3,
  "current_node_name": "财务审批",
  "pending_approver_person_ids": ["人员UUID"],
  "started_at": "2026-09-21T02:00:00Z",
  "idempotent_replay": false
}
```

`idempotent_replay=true` 表示本次请求命中了已有审批实例，没有创建新实例。

### 3.1 查询审批详情和时间线

```text
GET /api/approval-instances/{instance_id}
GET /api/approval-instances/{instance_id}/timeline
X-API-Key: appr_live_xxx
```

租户模式下，接口通过审批使用记录确认实例归属。只有发起该审批的租户可以读取详情和
时间线；缺少 API Key 返回 401，使用其他租户的 API Key 返回 403。流程授权后来被停用
不会影响已经创建的审批实例。全局模式下不要求 API Key。

## 4. 幂等约定

| 场景 | 结果 |
| --- | --- |
| 相同租户、流程和业务单号，请求内容一致 | 返回原审批实例，`idempotent_replay=true` |
| 相同租户、流程和业务单号，请求内容不同 | `409` |
| 相同业务单号，不同租户 | 各自创建实例，互不影响 |
| 并发提交相同业务单据 | 只产生一个实例和一条使用记录，另一个请求返回重放结果 |

参与内容比对的字段是标题、发起人、业务动作、`approval_form` 和 `execution_payload`。

## 5. 错误码

| 场景 | 状态码 |
| --- | --- |
| 缺少、无效、过期或撤销的 API Key | 401 |
| 租户已停用 | 401 |
| 租户没有流程授权或授权已停用 | 403 |
| 租户没有动作授权或授权已停用 | 403 |
| 审批流、业务动作不存在 | 404 |
| 同一业务单据使用不同内容重复发起 | 409 |
| 审批流状态不可用或业务动作已停用 | 409 |
| 审批表单校验失败 | 422 |
| 执行参数校验失败 | 422 |

无权访问时统一返回 403，响应内容不区分具体原因，也不会暴露其他租户是否绑定了该资源。

422 响应带结构化问题列表，`field` 可以直接定位到表单字段或执行参数字段：

```json
{
  "detail": {
    "message": "业务执行参数校验未通过",
    "issues": [
      {
        "code": "EXECUTION_PAYLOAD_INVALID",
        "message": "缺少必填配置项 payment_id",
        "field": "execution_payload",
        "node_id": null,
        "connection_index": null
      }
    ]
  }
}
```

## 6. 业务动作管理

```text
POST  /api/admin/business-actions
GET   /api/admin/business-actions?status=ENABLED&offset=0&limit=100
GET   /api/admin/business-actions/{action_id}
PATCH /api/admin/business-actions/{action_id}
```

创建请求：

```json
{
  "action_code": "PAYMENT_EXECUTE",
  "name": "执行付款",
  "description": "审批通过后调用付款系统执行付款",
  "http_method": "POST",
  "relative_path": "/payments/execute",
  "request_schema_json": {
    "type": "object",
    "required": ["payment_id", "amount"],
    "properties": {
      "payment_id": { "type": "string", "minLength": 1 },
      "amount": { "type": "number", "exclusiveMinimum": 0 }
    },
    "additionalProperties": false
  },
  "success_status_codes": [200, 201],
  "timeout_ms": 5000
}
```

约束：

- `action_code` 全局唯一，统一转换为大写。
- `http_method` 第一版只允许 `POST`、`PUT`、`PATCH`。
- `relative_path` 必须以单个 `/` 开头，不能是完整 URL 或 `//` 开头的协议相对地址。
- `request_schema_json` 必须是合法的 JSON Schema 且根类型为 `object`。
- `success_status_codes` 为空数组表示全部 2xx 视为调用成功。
- `PATCH` 只允许修改展示信息、调用配置和状态，`action_code` 创建后保持稳定。

业务动作停用后不能用于新申请，但不影响已经运行的审批实例。

## 7. 租户授权管理

```text
POST  /api/admin/tenants/{tenant_id}/process-bindings
GET   /api/admin/tenants/{tenant_id}/process-bindings
PATCH /api/admin/tenants/{tenant_id}/process-bindings/{binding_id}

POST  /api/admin/tenants/{tenant_id}/business-action-bindings
GET   /api/admin/tenants/{tenant_id}/business-action-bindings
PATCH /api/admin/tenants/{tenant_id}/business-action-bindings/{binding_id}
```

- 创建请求体分别是 `{"process_id": "..."}` 和 `{"business_action_id": "..."}`。
- 创建前会确认流程或业务动作存在，不存在返回 404。
- 重复创建返回同一条授权记录；已经停用的授权会被重新启用。
- `PATCH` 请求体为 `{"status": "ENABLED"}` 或 `{"status": "DISABLED"}`。
- 授权记录不提供物理删除，停用只阻止新申请，不影响已经运行的审批实例。

## 8. 审批使用记录

```text
GET /api/admin/tenants/{tenant_id}/process-usage-records
GET /api/admin/tenants/{tenant_id}/process-usage-records/{record_id}
```

支持的筛选条件：

```text
process_id    流程 ID
business_key  业务单号
action_code   业务动作标识
created_from  发起时间下界
created_to    发起时间上界
offset/limit  分页
```

响应示例：

```json
{
  "id": "使用记录UUID",
  "tenant_id": "租户UUID",
  "process_id": "审批流UUID",
  "process_version_id": "发起时使用的版本UUID",
  "approval_instance_id": "审批实例UUID",
  "business_key": "PAY-20260921-001",
  "action_code": "PAYMENT_EXECUTE",
  "created_at": "2026-09-21T02:00:00Z",
  "approval_status": "RUNNING",
  "approval_title": "供应商付款申请",
  "current_node_name": "财务审批",
  "started_at": "2026-09-21T02:00:00Z",
  "finished_at": null,
  "duration_ms": 120000
}
```

`approval_status`、`approval_title`、`current_node_name` 和耗时在查询时从 `process`
运行表读取后组装，使用记录表只保存归属和关联信息，不重复保存运行状态。
