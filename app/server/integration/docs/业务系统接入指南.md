# 业务系统接入审批中心指南

> 文档状态：与当前第一版实现一致  
> 面向对象：需要向审批中心发起审批并在审批通过后接收业务回调的业务系统开发人员  
> 最后更新：2026-09-23

## 1. 接入目标

业务系统接入后，只需要负责自己的业务单据和业务执行接口，不需要维护审批流节点、审批人
或审批规则。审批中心负责流程配置、任务分发、审批推进和审批记录。

完整链路如下：

```text
业务系统创建业务单据
    → 使用 X-API-Key 向审批中心发起审批
    → 审批中心按照 process_id 运行当前已发布流程
    → 审批人在审批中心处理任务
    → 业务系统查询审批状态
    → 审批通过后，审批中心使用 Service Token 回调业务系统
    → 业务系统幂等执行业务动作并返回 HTTP 结果
    → 审批中心保存本次回调结果
```

审批流由审批中心统一维护。业务系统只保存和使用审批中心分配的稳定 `process_id`，不需要
保存流程版本，也不需要在自己的数据库中复制一套流程定义。

## 2. 双方职责

| 审批中心负责 | 业务系统负责 |
| --- | --- |
| 创建租户并签发 API Key | 安全保存 API Key，并只在服务端调用审批中心 |
| 维护人员和审批人 | 提供申请人与审批中心人员 ID 的映射 |
| 创建、发布和维护审批流 | 在发起审批时选择约定的 `process_id` |
| 配置业务动作和参数规则 | 按约定组装 `approval_form` 和 `execution_payload` |
| 分发审批任务并记录审批过程 | 保存返回的 `approval_instance_id` |
| 审批通过后调用业务接口 | 提供可访问且幂等的业务执行接口 |
| 保存回调状态、响应和错误 | 为审批中心签发长期 Service Token |

## 3. 两种凭据不要混用

| 凭据 | 调用方向 | 用途 | 默认请求头 |
| --- | --- | --- | --- |
| API Key | 业务系统 → 审批中心 | 确定业务系统所属租户 | `X-API-Key` |
| Service Token | 审批中心 → 业务系统 | 审批通过后调用业务接口 | `Authorization: Bearer ...` |

API Key 由审批中心签发。Service Token 由业务系统为审批中心专用服务账号签发，再交给审批
中心加密保存。Service Token 不应使用登录用户 Token，因为用户 Token 通常会过期，也可能
依赖具体用户身份。

`X-Admin-Key` 只用于审批中心的临时管理接口，不提供给普通业务调用链路使用。

## 4. 接入前需要确认的信息

业务系统需要向审批中心提供：

| 信息 | 示例 | 说明 |
| --- | --- | --- |
| 系统编码 | `PAYMENT` | 租户稳定编码 |
| 系统名称 | `付款系统` | 管理端展示名称 |
| 回调基础地址 | `https://payment.example.com` | 不能包含查询参数和 URL 片段 |
| 回调相对路径 | `/api/approval/payment/execute` | 与基础地址拼成最终地址 |
| HTTP 方法 | `POST` | 第一版支持 `POST`、`PUT`、`PATCH` |
| 请求参数规则 | JSON Schema | 用于校验 `execution_payload` |
| 成功状态码 | `[200, 201]` | 未单独配置时全部 2xx 都视为成功 |
| 超时时间 | `5000` 毫秒 | 可配置范围为 1 至 300000 毫秒 |
| Service Token | 业务系统签发 | 供审批中心回调时认证 |
| 认证头和前缀 | `Authorization`、`Bearer` | 也可以配置其他请求头或空前缀 |

审批中心配置完成后向业务系统提供：

- 审批中心环境地址。
- 租户 API Key。
- 可使用的 `process_id`。
- 可使用的 `action_code`。
- `approval_form` 的表单字段约定。
- `execution_payload` 的参数字段约定。
- 测试环境的人员 UUID。

## 5. 第一步：验证 API Key

```http
GET {APPROVAL_BASE_URL}/api/tenant/context
X-API-Key: appr_live_xxx
```

成功响应统一使用以下包装：

```json
{
  "code": 0,
  "msg": "success",
  "data": {
    "tenant_id": "租户UUID",
    "tenant_code": "PAYMENT",
    "tenant_name": "付款系统",
    "api_key_id": "API Key UUID"
  }
}
```

缺少、无效、过期或已撤销的 API Key 返回 HTTP 401。业务系统不应把 API Key 放在浏览器
代码、URL 查询参数或业务日志中。

## 6. 第二步：发起审批

### 6.1 请求

```http
POST {APPROVAL_BASE_URL}/api/processes/{process_id}/instances
Content-Type: application/json
X-API-Key: appr_live_xxx
```

```json
{
  "business_key": "PAY-20260923-001",
  "title": "供应商付款申请",
  "applicant_person_id": "申请人在审批中心的人员UUID",
  "action_code": "PAYMENT_EXECUTE",
  "approval_form": {
    "supplier_name": "示例供应商",
    "amount": 10000,
    "reason": "采购付款"
  },
  "execution_payload": {
    "payment_id": "PAY-20260923-001",
    "amount": 10000
  }
}
```

字段说明：

| 字段 | 必填 | 说明 |
| --- | --- | --- |
| `business_key` | 是 | 业务系统自己的稳定单据标识，最长 200 个字符 |
| `title` | 是 | 审批单标题，最长 200 个字符 |
| `applicant_person_id` | 否 | 审批中心人员 UUID；第一版人员由管理端手工维护 |
| `action_code` | 否 | 审批通过后执行的业务动作；为空表示只审批、不回调 |
| `approval_form` | 按流程要求 | 给审批人查看和做分支判断的数据；省略时按空对象处理 |
| `execution_payload` | 按动作要求 | 审批通过后原样发送给业务接口的 JSON 对象；省略时按空对象处理 |

`approval_form` 和 `execution_payload` 的用途不同，即使部分字段重复也应分别提交。审批中心
会分别按照流程表单 Schema 和业务动作 Schema 校验并保存。

回调请求体只包含 `execution_payload`。如果业务接口需要 `business_key`、申请人或其他字段，
业务系统必须把这些字段明确放入 `execution_payload`，不能假设审批中心会自动补充。

### 6.2 成功响应

```json
{
  "code": 0,
  "msg": "success",
  "data": {
    "instance_id": "审批实例UUID",
    "status": "RUNNING",
    "process_id": "审批流UUID",
    "process_version_id": "实际使用的版本UUID",
    "process_version_no": 3,
    "current_node_name": "财务审批",
    "pending_approver_person_ids": ["审批人UUID"],
    "started_at": "2026-09-23T02:00:00Z",
    "idempotent_replay": false
  }
}
```

业务系统至少应保存 `instance_id`，用于后续查询审批状态。业务系统仍然只配置稳定的
`process_id`；`process_version_id` 仅表示本次实例实际使用的已发布版本。

### 6.3 发起请求的幂等规则

幂等范围是：

```text
租户 + process_id + business_key
```

- 相同范围、请求内容完全一致：返回原实例，`idempotent_replay=true`。
- 相同范围、标题、申请人、动作或两份 JSON 数据不同：返回 HTTP 409。
- 网络超时后无法确定是否创建成功：使用完全相同的请求再次提交。
- 不要通过生成新的 `business_key` 来重试，否则会创建第二个审批实例。

建议直接使用业务系统数据库中不会变化的单据主键作为 `business_key`。

## 7. 第三步：查询审批结果

### 7.1 查询详情

```http
GET {APPROVAL_BASE_URL}/api/approval-instances/{instance_id}
X-API-Key: appr_live_xxx
```

### 7.2 查询完整时间线

```http
GET {APPROVAL_BASE_URL}/api/approval-instances/{instance_id}/timeline
X-API-Key: appr_live_xxx
```

审批实例状态：

| 状态 | 含义 |
| --- | --- |
| `RUNNING` | 正在审批 |
| `APPROVED` | 审批通过 |
| `REJECTED` | 审批拒绝 |
| `CANCELLED` | 实例被取消或终止 |
| `ERROR` | 流程运行异常 |

第一版只有“审批通过后的业务动作回调”，审批拒绝时不会主动通知业务系统。如果业务系统
需要及时更新自己的审批状态，需要定时查询详情，或者由上层项目平台在用户打开单据时查询。
后续可以增加统一的审批结果事件通知，但不属于当前第一版闭环。

建议业务系统把“审批状态”和“业务执行状态”分开理解：审批实例进入 `APPROVED`，只表示
审批决策已经通过，并不代表业务回调一定成功。

## 8. 第四步：实现业务回调接口

假设审批中心配置为：

```text
callback_base_url = https://payment.example.com
relative_path     = /api/approval/payment/execute
http_method       = POST
header_name       = Authorization
token_prefix      = Bearer
```

审批通过后会发出：

```http
POST https://payment.example.com/api/approval/payment/execute
Content-Type: application/json
Authorization: Bearer {SERVICE_TOKEN}
```

请求体就是发起审批时保存的 `execution_payload`：

```json
{
  "payment_id": "PAY-20260923-001",
  "amount": 10000
}
```

业务接口应按以下顺序处理：

1. 校验 Service Token，并确认它属于审批中心服务账号。
2. 校验业务单据存在且当前状态允许执行。
3. 使用 `payment_id` 等稳定业务标识做幂等检查。
4. 在业务系统自己的事务中执行业务动作。
5. 重复请求已执行成功时，直接返回成功，不重复付款或重复修改数据。
6. 返回配置中约定的 HTTP 成功状态码。

审批中心第一版只根据 HTTP 状态码判断成功，不解析响应 JSON 中的业务码。例如返回 HTTP 200
但正文是 `{"code": 500}`，审批中心仍会记录为成功。因此业务系统必须使用正确的 HTTP
状态码表达结果。

审批中心不会跟随 3xx 重定向。响应正文只用于排查并会截断保存，不能依靠大响应体传递后续
流程数据。

## 9. 回调可靠性约定

- 审批通过事务中只创建执行记录，HTTP 回调由后台 Worker 异步发送。
- 当前第一版尽力调用一次，不自动重试 `FAILED` 记录。
- 网络失败、超时或非成功状态码会保存为 `FAILED`，但不会把审批状态从 `APPROVED` 改回去。
- 业务接口仍然必须幂等，为以后增加人工重试或自动重试做好准备。
- Service Token 过期、撤销或配置缺失会导致回调失败。
- 更换 Service Token 时，审批中心创建新凭据并在同一事务中撤销旧凭据。

业务执行记录目前属于管理接口，使用 `X-Admin-Key` 查询。业务系统侧暂时不能使用自己的
API Key 查询执行记录；它可以根据是否收到回调判断执行情况，审批中心后台人员可以查询
执行失败原因。后续如业务方确实需要，可增加按租户隔离的执行记录查询接口。

## 10. 常见错误与处理方式

| HTTP 状态码 | 常见原因 | 业务系统处理 |
| --- | --- | --- |
| 401 | API Key 缺失、无效、过期或撤销 | 停止重试并联系审批中心更换凭据 |
| 403 | 租户未获得流程或业务动作授权 | 联系审批中心检查绑定关系 |
| 404 | `process_id`、`action_code` 或实例不存在 | 检查环境和配置，不要盲目重试 |
| 409 | 流程不可用，或相同业务单号提交了不同内容 | 查询原实例或修正业务状态 |
| 422 | 表单或执行参数不符合 Schema | 根据问题字段修正请求 |
| 5xx | 审批中心暂时异常 | 使用相同 `business_key` 和完全相同的内容重试 |

业务系统应设置调用超时并记录审批中心返回的 HTTP 状态、错误详情和请求关联信息，但日志中
不得记录完整 API Key 或 Service Token。

## 11. 业务系统建议保存的字段

以下字段不是审批中心强制的数据表设计，只是便于业务系统管理审批状态的建议：

| 字段 | 作用 |
| --- | --- |
| `approval_process_id` | 本业务类型使用的审批流 ID |
| `approval_instance_id` | 审批中心实例 ID |
| `approval_status` | 最近一次查询到的审批状态 |
| `approval_submitted_at` | 发起审批时间 |
| `approval_finished_at` | 审批结束时间 |
| `approval_action_code` | 本次申请使用的业务动作 |

业务系统不需要保存 `process_version_id` 来控制运行。审批中心会保证已经发起的实例继续使用
原版本，新申请自动使用流程当前已发布版本。

## 12. 接入检查清单

### 审批中心侧

- [ ] 已创建并启用租户。
- [ ] 已登记业务系统可访问的回调基础地址。
- [ ] 已签发并交付测试环境 API Key。
- [ ] 已保存业务系统签发的 Service Token。
- [ ] 已维护申请人和审批人。
- [ ] 已发布审批流并交付 `process_id`。
- [ ] 已授权租户使用审批流。
- [ ] 已创建业务动作并交付 `action_code`。
- [ ] 已授权租户使用业务动作。
- [ ] 已确认两份 JSON Schema。

### 业务系统侧

- [ ] API Key 只保存在服务端安全配置中。
- [ ] 能使用稳定 `business_key` 发起审批。
- [ ] 会保存审批中心返回的 `instance_id`。
- [ ] 网络结果不确定时会使用完全相同的请求进行幂等重试。
- [ ] 能查询审批详情并处理 `APPROVED`、`REJECTED` 和异常状态。
- [ ] 已实现并开放业务回调接口。
- [ ] 回调接口能够校验 Service Token。
- [ ] 回调接口按照稳定业务单号实现幂等。
- [ ] 回调接口使用正确的 HTTP 状态码表示成功或失败。
- [ ] 日志不会输出 API Key 和 Service Token 明文。

## 13. 当前第一版限制

- 暂不支持审批附件和文件上传。
- 暂不提供审批拒绝事件回调，业务系统需要查询审批状态。
- 暂不自动重试失败的业务执行记录。
- 暂不允许业务系统使用 API Key 查询业务执行记录。
- 人员和组织数据暂时由审批中心手工维护。
- 审批任务接口暂时显式传递人员 ID，后续接入项目平台登录身份后替换。

这些限制不影响“发起审批、人工审批、通过后执行、保存执行结果”的第一版主链路。
