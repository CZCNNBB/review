# 业务系统与审批中心交互接入指南

> 文档状态：与当前第一版实现一致
>
> 面向对象：业务系统后端、业务前端和联调人员
>
> 最后更新：2026-09-24

## 1. 核心接入方式

业务系统不需要维护审批流、审批节点、审批人或流程推进代码。用户仍然在原业务系统的
前端页面点击“发起审批”，但浏览器不直接携带 API Key 调用审批中心，而是先调用业务
系统自己的后端，由业务后端读取可信业务数据并转发给审批中心。

```text
业务系统前端
    → 业务系统后端的“发起审批”接口
    → 审批中心发起接口
    → 审批中心完成任务分发和流程推进
    → 审批通过
    → 审批中心调用业务系统的“执行业务动作”接口
    → 业务系统幂等执行真实业务
```

以付款系统为例：

```text
用户在付款单页面点击“发起审批”
    → POST /api/payments/{payment_id}/submit-approval
    → 付款系统后端读取真实付款单
    → 付款系统后端调用审批中心
    → 财务人员在审批中心审批
    → 审批中心回调付款系统执行接口
    → 付款系统重新读取付款单并执行付款
```

这种方式下，业务系统只需要做少量适配，审批能力仍然集中在审批中心。

## 2. 为什么不能让浏览器直接调用审批中心

当前审批中心使用 `X-API-Key` 识别业务系统租户。API Key 是系统级凭据，不能写入前端
代码、浏览器存储或页面请求中，否则用户可以获取它并伪造该业务系统的审批申请。

另外，金额、收款方、合同编号等审批数据必须来自业务系统数据库。如果浏览器直接组装
这些数据，用户可以修改请求内容，使审批人看到的数据与真实业务单据不一致。

因此约定：

- 业务前端只向自己的业务后端提交业务单据 ID。
- 业务后端根据当前登录用户检查发起权限。
- 业务后端从数据库读取审批展示数据和执行参数。
- API Key 只保存在业务后端的安全配置中。
- 业务后端调用审批中心，而不是把 API Key 返回给浏览器。

## 3. 双方职责

| 审批中心负责 | 业务系统负责 |
| --- | --- |
| 创建并维护审批流 | 维护自己的业务单据 |
| 维护节点、审批人和审批规则 | 判断业务单据是否允许发起审批 |
| 为业务系统签发 API Key | 安全保存 API Key |
| 接收审批申请并创建审批实例 | 提供一个薄的发起审批接口 |
| 分发待办并推进审批流程 | 保存返回的审批实例 ID |
| 保存审批表单和审批记录 | 提供可信的审批展示数据 |
| 审批通过后发起业务回调 | 提供幂等的业务执行接口 |
| 保存回调结果和失败原因 | 为审批中心签发 Service Token |

## 4. 业务系统需要提供的两个接口

### 4.1 发起审批接口

该接口由业务系统提供给自己的前端使用，不对审批中心开放。

付款系统示例：

```http
POST /api/payments/{payment_id}/submit-approval
Authorization: 当前登录用户凭据
```

前端只需要传付款单 ID，通常不需要重复传金额、收款方等数据库中已经存在的数据。

业务后端在该接口中完成：

1. 校验当前登录用户是否有权操作该付款单。
2. 查询付款单并确认当前状态允许发起审批。
3. 从数据库读取金额、供应商和付款原因等可信数据。
4. 组装审批中心需要的 `approval_form`。
5. 组装审批通过后使用的 `execution_payload`。
6. 从服务端配置读取 API Key、`process_id` 和 `action_code`。
7. 调用审批中心发起接口。
8. 保存返回的 `approval_instance_id` 和审批状态。
9. 把发起结果返回业务前端。

该接口只承担业务校验、数据转换和转发，不实现任何审批流程逻辑。

### 4.2 执行业务动作接口

该接口由业务系统提供给审批中心调用，只允许经过 Service Token 认证的审批中心服务账号
访问。

付款系统示例：

```http
POST /api/internal/approval/payments/execute
Authorization: Bearer {SERVICE_TOKEN}
Content-Type: application/json
```

```json
{
  "payment_id": "PAY-20260924-001"
}
```

业务系统在该接口中完成：

1. 校验 Service Token。
2. 根据 `payment_id` 重新读取真实付款单。
3. 检查付款单已经发起审批且当前状态允许执行。
4. 按业务单号执行幂等检查。
5. 在业务系统自己的事务中执行付款。
6. 重复调用时返回原成功结果，不重复付款。
7. 使用正确的 HTTP 状态码表达执行结果。

执行接口不能直接相信请求中的金额、收款账号等关键字段。最简单可靠的方式是回调只传
稳定的业务单据 ID，由业务系统重新读取自己的数据库。

## 5. 两种服务凭据

| 凭据 | 调用方向 | 用途 | 保存位置 |
| --- | --- | --- | --- |
| API Key | 业务系统后端 → 审批中心 | 识别业务系统所属租户 | 业务系统后端安全配置 |
| Service Token | 审批中心 → 业务系统后端 | 调用业务执行接口 | 审批中心加密保存 |

API Key 由审批中心签发。Service Token 由业务系统为审批中心服务账号签发。

用户登录 Token 不适合作为 Service Token，因为用户 Token 通常会过期，并且依赖某个具体
用户。Service Token 应当代表审批中心这个系统身份。

## 6. 业务后端如何调用审批中心

### 6.1 发起审批

```http
POST {APPROVAL_BASE_URL}/api/processes/{process_id}/instances
X-API-Key: appr_live_xxx
Content-Type: application/json
```

```json
{
  "business_key": "PAY-20260924-001",
  "title": "供应商付款申请",
  "applicant_person_id": "申请人在审批中心的人员UUID",
  "action_code": "PAYMENT_EXECUTE",
  "approval_form": {
    "supplier_name": "示例供应商",
    "amount": 10000,
    "reason": "采购付款"
  },
  "execution_payload": {
    "payment_id": "PAY-20260924-001"
  }
}
```

字段说明：

| 字段 | 说明 |
| --- | --- |
| `business_key` | 业务系统稳定且唯一的业务单据标识 |
| `title` | 审批单标题 |
| `applicant_person_id` | 当前申请人在审批中心中的人员 UUID |
| `action_code` | 审批通过后执行的动作；为空表示只审批、不执行 |
| `approval_form` | 给审批人查看并用于流程条件判断的数据 |
| `execution_payload` | 审批通过后原样发送给业务执行接口的数据 |

建议 `execution_payload` 只保存执行业务所需的稳定标识。审批中心会保存它，但业务执行接口
仍应重新读取并校验业务系统数据库中的真实数据。

### 6.2 发起成功响应

```json
{
  "code": 0,
  "msg": "success",
  "data": {
    "instance_id": "审批实例UUID",
    "status": "RUNNING",
    "process_id": "审批流UUID",
    "process_version_id": "本次运行使用的版本UUID",
    "process_version_no": 3,
    "current_node_name": "财务审批",
    "pending_approver_person_ids": ["审批人UUID"],
    "started_at": "2026-09-24T02:00:00Z",
    "idempotent_replay": false
  }
}
```

业务系统至少保存：

- `instance_id`
- `status`
- `started_at`

业务系统不需要维护 `process_version_id`。业务系统始终传稳定的 `process_id`，审批中心自动
选择当前已发布版本，并保证已经运行的实例不受新版本影响。

## 7. 业务系统发起接口参考逻辑

下面是与语言和框架无关的伪代码：

```python
def submit_payment_approval(payment_id, current_user):
    # 读取真实业务单据，不能使用浏览器提交的金额作为审批依据。
    payment = payment_repository.get(payment_id)
    ensure_user_can_submit(current_user, payment)
    ensure_payment_can_start_approval(payment)

    approval_request = {
        "business_key": payment.id,
        "title": f"付款申请：{payment.id}",
        "applicant_person_id": current_user.approval_person_id,
        "action_code": "PAYMENT_EXECUTE",
        "approval_form": {
            "supplier_name": payment.supplier_name,
            "amount": payment.amount,
            "reason": payment.reason,
        },
        "execution_payload": {
            "payment_id": payment.id,
        },
    }

    result = approval_client.start_process(
        process_id=PAYMENT_PROCESS_ID,
        api_key=APPROVAL_API_KEY,
        request=approval_request,
    )

    payment.approval_instance_id = result.instance_id
    payment.approval_status = result.status
    payment.approval_submitted_at = result.started_at
    payment_repository.save(payment)
    return result
```

## 8. 幂等和重复提交

审批中心的发起幂等范围为：

```text
租户 + process_id + business_key
```

- 相同业务单号和相同请求内容重复提交：返回原审批实例。
- 相同业务单号但请求内容不同：返回 HTTP 409。
- 调用审批中心超时且不知道是否成功：使用完全相同的请求重试。
- 不要为了重试而生成新的 `business_key`，否则会产生第二个审批实例。

业务系统自己的发起接口也应该先检查业务单据是否已经保存 `approval_instance_id`。已经发起
时可以直接返回原实例信息，避免浏览器连续点击产生无意义的请求。

业务执行接口必须独立实现幂等。即使当前审批中心第一版不自动重试，也需要为网络重试、
人工补偿和后续自动重试能力提前保证安全。

## 9. 审批状态如何同步回业务系统

业务后端使用保存的 `approval_instance_id` 查询：

```http
GET {APPROVAL_BASE_URL}/api/approval-instances/{instance_id}
X-API-Key: appr_live_xxx
```

如需展示完整处理过程，可以查询：

```http
GET {APPROVAL_BASE_URL}/api/approval-instances/{instance_id}/timeline
X-API-Key: appr_live_xxx
```

业务系统前端同样不直接携带 API Key。可以由业务系统已有的业务详情接口返回最近保存的
审批状态，或者让业务后端在查询业务详情时同步查询审批中心。

审批实例主要状态：

| 状态 | 含义 |
| --- | --- |
| `RUNNING` | 正在审批 |
| `APPROVED` | 审批通过 |
| `REJECTED` | 审批拒绝 |
| `CANCELLED` | 实例被取消或终止 |
| `ERROR` | 流程运行异常 |

第一版审批拒绝时不会主动回调业务系统，因此业务系统如果需要及时展示拒绝结果，需要通过
详情查询接口同步状态。后续可以再增加统一的审批结果事件通知。

## 10. 审批通过后的回调规则

审批中心使用以下配置组成业务请求：

```text
租户 callback_base_url + 业务动作 relative_path
```

例如：

```text
callback_base_url = https://payment.example.com
relative_path     = /api/internal/approval/payments/execute
最终调用地址      = https://payment.example.com/api/internal/approval/payments/execute
```

审批中心发送的请求体就是发起审批时保存的 `execution_payload`，不会自动添加
`business_key`、`approval_instance_id` 或其他字段。业务执行需要哪些稳定标识，业务后端就
必须在发起时把它们放进 `execution_payload`。

审批中心第一版只根据 HTTP 状态码判断执行成功：

- 配置了成功状态码时，只有命中的状态码算成功。
- 没有配置时，全部 2xx 算成功。
- 不解析响应正文中的业务状态码。
- 不跟随 3xx 重定向。
- 超时、网络异常或非成功状态码记录为 `FAILED`。
- 执行失败不会把已经 `APPROVED` 的审批改为拒绝。

例如业务系统返回 HTTP 200，但正文是 `{"code": 500}`，审批中心仍会认为执行成功。因此
业务接口必须使用正确的 HTTP 状态码表达结果。

## 11. 业务系统建议保存的字段

业务表可以增加以下字段，字段名称由业务系统自行决定：

| 字段 | 用途 |
| --- | --- |
| `approval_instance_id` | 审批中心实例 ID |
| `approval_status` | 最近一次同步的审批状态 |
| `approval_process_id` | 当前业务类型使用的流程 ID |
| `approval_action_code` | 审批通过后执行的业务动作 |
| `approval_submitted_at` | 发起审批时间 |
| `approval_finished_at` | 审批结束时间 |

业务系统不需要保存流程节点、审批人、任务或流程版本配置。

## 12. 接入配置清单

### 审批中心提供给业务系统

- [ ] 审批中心环境地址。
- [ ] 租户 API Key。
- [ ] 业务类型对应的 `process_id`。
- [ ] 审批通过后使用的 `action_code`。
- [ ] `approval_form` 字段约定。
- [ ] `execution_payload` 字段约定。
- [ ] 测试环境人员 UUID。

### 业务系统提供给审批中心

- [ ] 回调基础地址。
- [ ] 业务执行接口相对路径和 HTTP 方法。
- [ ] 执行参数 JSON Schema。
- [ ] 回调成功状态码和超时时间。
- [ ] Service Token、认证请求头名称和 Token 前缀。
- [ ] 测试业务单据和联调联系人。

### 业务系统开发内容

- [ ] 前端调用业务后端的发起审批接口。
- [ ] 业务后端从数据库读取可信业务数据。
- [ ] 业务后端调用审批中心并保存 `approval_instance_id`。
- [ ] 业务后端支持查询或同步审批状态。
- [ ] 业务后端提供 Service Token 保护的执行接口。
- [ ] 业务执行接口按照业务单号实现幂等。
- [ ] API Key 和 Service Token 不进入浏览器或普通日志。

## 13. 最小改造范围

对于一个已经存在的业务系统，第一版通常只需要：

1. 增加一组审批中心服务端配置：API Key、`process_id`、`action_code`。
2. 增加一个供业务前端调用的发起审批接口。
3. 增加或复用一个供审批中心调用的业务执行接口。
4. 在业务单据中保存审批实例 ID 和审批状态。
5. 在业务详情查询中返回或同步审批状态。

业务系统不需要实现流程引擎，也不需要维护审批节点和审批人配置。

## 14. 当前第一版限制

- 暂不支持审批附件和文件上传。
- 暂不提供审批拒绝事件回调，业务系统需要查询审批状态。
- 暂不自动重试失败的业务执行记录。
- 暂不允许业务系统使用 API Key 查询业务执行记录。
- 人员和组织数据暂时由审批中心手工维护。
- 审批任务接口暂时显式传递人员 ID，后续接入项目平台登录身份后替换。
